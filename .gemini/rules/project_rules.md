# Project Rules for Menstrual Cycle Tracker

- **Auto Push:** Tự động thực hiện `git add`, `git commit` và `git push` sau khi hoàn thành công việc và kiểm tra build thành công (chỉ áp dụng riêng cho project này theo yêu cầu của user).
